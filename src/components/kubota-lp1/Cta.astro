---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a class="k1-cta" href={href} target="_blank" rel="sponsored nofollow noopener">
  <strong>{label}</strong>
</a>
